import 'package:flutter/material.dart';

class AppColors {
  // Colores principales
  static const Color primaryPurple = Color(0xFF34003B);
  static const Color accentGreen = Color(0xFF66FF00);

  // Colores básicos
  static const Color black = Color(0xFF000000);
  static const Color white = Color(0xFFFFFFFF);
  static const Color boneWhite = Color(0xFFD9D9D9); // usado en Pokedle
  static const Color grey = Colors.grey;

  // Colores de estado
  static const Color errorRed = Color(0xFFFF0000);

  // Colores extra
  static const Color accentBlue = Color(0xFF005DFE);
  static const Color neutralGray = Color(0xFF475C80);
}
