import 'package:flutter/material.dart';

class GuessTypePage extends StatelessWidget {
  const GuessTypePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}